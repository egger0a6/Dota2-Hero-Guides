<%- include('./partials/html-head') %>
<%- include('./partials/nav') %>

<main>
  <h1>Greetings, <%= user ? user.profile.name : "traveler" %></h1>
  <% if (user?.profile.avatar) { %>
    <div>
      <img
        src="<%= user.profile.avatar %>"
        alt="<%= user.profile.name %> avatar"
      />
    </div>
  <% } %>
</main>

<%- include('./partials/footer') %>
